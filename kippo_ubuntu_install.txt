sudo apt-get update

sudo apt-get install python-dev openssl python-openssl python-pyasn1 python-twisted python-crypto python-zope.interface

sudo useradd -d /home/kippo -s /bin/bash -m kippo -g sudo

sudo apt-get install authbind

sudo passwd kippo

sudo apt-get install unzip

sudo wget https://github.com/name/*zip

sudo apt-get install mysql-server

mysql -u root -p

CREATE DATABASE databasename;

SHOW DATABASES;

GRANT ALL PRIVILEGES ON databasename.* TO "kippo"@"localhost" IDENTIFIED BY "password";

FLUSH PRIVILEGES;

SELECT User, host FROM mysql.user;

